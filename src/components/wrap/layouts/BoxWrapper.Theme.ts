// src/components/wrap/layouts/BoxWrapper.Theme.ts
import {Theme} from '@mui/material/styles'

export const BoxWrapperTheme = (theme: Theme) => ({
  backgroundColor: theme.palette.background.default // Usando uma cor do tema
})
