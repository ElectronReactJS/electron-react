// src/component/TabsContent.js

import React from 'react';

const TabsContent = ({ children }) => {
    // Este componente não precisa renderizar nada por si só,
    // pois sua renderização é gerenciada pelo TabsLayout.
    return null;
};

export default TabsContent;
